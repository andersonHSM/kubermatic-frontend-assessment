## Environment variables for docker-compose
# Copy this file to .env and adjust values as needed

# Postgres
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=applocalpw
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# API
API_PORT=3000
NODE_ENV=production

# CORS settings (comma-separated). Allow both Angular dev and web container ports
# Example: http://localhost:4200 (Angular), http://localhost:8080 (web in Docker)
CORS_ORIGINS=http://localhost:4200,http://localhost:8080

# JWT (NestJS)
# Secret key used to sign JWT tokens (change in production!)
JWT_SECRET=change-me-in-prod
# Token expiration time
JWT_EXPIRES_IN=3600

# Web
WEB_PORT=8080

# Full connection string (overrides individual POSTGRES_* if set in compose)
# Compose will pass this value directly to the API container.
DATABASE_URL=postgres://appuser:applocalpw@postgres:5432/appdb
